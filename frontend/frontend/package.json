{
  "name": "frontend-alias",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd .. && npm run build && node -e \"const fs=require('fs'); if(fs.existsSync('dist')){fs.cpSync('dist', 'frontend/dist', {recursive:true});}\""
  }
}
