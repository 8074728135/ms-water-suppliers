{
  "buildCommand": "node -e \"const fs=require('fs'),cp=require('child_process');if(fs.existsSync('frontend/package.json')){cp.execSync('cd frontend && npm install && npm run build',{stdio:'inherit'})}else{cp.execSync('npm run build',{stdio:'inherit'})}\"",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "https://ms-water-suppliers.onrender.com/api/:match*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
